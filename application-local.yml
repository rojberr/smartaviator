# Postgres database connection details
spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/smartaviator-db"
    username: "postgres"
    password: "mypass"
  # Creation of database
  jpa:
    hibernate:
      ddl-auto: "create-drop"